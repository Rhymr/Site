{
  "name": "rhymr-checkout",
  "version": "0.1.0",
  "private": true,
  "description": "Stripe Checkout backend for the Rhymr macOS build (pay-what-you-want, $5 minimum).",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "stripe:listen": "stripe listen --forward-to localhost:4242/api/webhook"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  }
}
